import { Canvas, Meta } from "@storybook/blocks";

import * as MediaStories from "./components/Media/Media.stories";

<Meta of={MediaStories} />

# Media

A description about the Media component.

Proposed new schema;

---

{/_ `media / backgroundMedia: {
  image: {
    src: string,
    id: string | number,
    caption: string,
    imageAlt: string,
    isSVG: boolean
  },
  video: {
    src: string,
    id: string | number,
    type: vimeo | youtube | undefined // if undefined, assumes umbraco.
    autoPlay: boolean,
    loop: boolean,
    allowFullscreen: boolean,
    allowControls: boolean,
    allowSound: boolean
  } | null
}` _/}

---

<Canvas of={MediaStories.MediaImage} />
<Canvas of={MediaStories.MediaVideo} />
