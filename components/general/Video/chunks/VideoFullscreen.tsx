'use client';
import { FC } from 'react';
import { createPortal } from 'react-dom';

const VideoFullscreen: FC<any> = ({ data }: any) => {
  if (!data) return null;

  return null;
};

export default VideoFullscreen;
